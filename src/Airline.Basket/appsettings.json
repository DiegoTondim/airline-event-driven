{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Messaging": {
    "Default": {
      "Endpoint": "amqp://localhost:5672",
      "UserName": "guest",
      "Password": "guest"
    },
    "Resources": {
      "Endpoint": "amqp://localhost:5672/resources",
      "UserName": "guest",
      "Password": "guest"
    }
  }
}
